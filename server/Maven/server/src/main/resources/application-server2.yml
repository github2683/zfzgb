server:
  port: 8003 #注册中心端口
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false #不自己注册自己
    fetch-registry: false #不需要去检索服务
    service-url:
      defaultZone:  http://${eureka.instance.hostname}:8002/eureka/ #注册中心地址
